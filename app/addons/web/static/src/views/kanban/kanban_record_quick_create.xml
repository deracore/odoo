<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.KanbanRecordQuickCreate">
        <KanbanQuickCreateController t-if="state.isLoaded" t-props="quickCreateProps" />
    </t>

    <t t-name="web.KanbanQuickCreateController">
        <div
            class="app_kanban_quick_create app_field_highlight shadow"
            t-att-class="{ app_disabled: state.disabled, 'w-100 mx-0': props.listIsGrouped }"
            t-ref="root"
        >
            <t t-component="props.Renderer" class="'app_form_view app_xxs_form_view app_kanban_quick_create_form p-0'" record="model.root" Compiler="props.Compiler" archInfo="props.archInfo"/>
            <div class="d-flex flex-wrap justify-content-end gap-1">
                <button class="btn btn-primary app_kanban_add me-1" t-on-click="() => this.validate('add')">
                    Add
                </button>
                <button class="btn btn-secondary app_kanban_edit me-1" t-on-click="() => this.validate('edit')">
                    Edit
                </button>
                <span class="flex-grow-1"></span>
                <button class="btn btn-secondary app_kanban_cancel" t-on-click="() => this.cancel(true)">
                    <i class="fa fa-trash" />
                </button>
            </div>
        </div>
    </t>

</templates>
